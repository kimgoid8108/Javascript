const age = Number(window.prompt("나이를 입력하시오"));
const result = age >= 20 ? "성인입니다" : "미성년자입니다";

window.console.log(result);

const height = Number(window.prompt("키를 입력하시오"));
const result1 = height >= 140 ? "가능합니다" : "탑승이 안됩니다";

window.console.log(result1);

const ture = Number(window.prompt("수를 입력하시오"));
const result2 = true > 0 ? "양수입니다" : "0 또는 음수입니다";

window.console.log(result2);
